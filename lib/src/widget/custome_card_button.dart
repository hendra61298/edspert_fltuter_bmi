
import 'package:flutter/cupertino.dart';

class CustomCardButton extends StatelessWidget {
  const CustomCardButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
